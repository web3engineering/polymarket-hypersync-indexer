# Copy to .env.test and fill in your token, then:
#   export $(cat .env.test | xargs) && bash scripts/run-tests.sh

ENVIO_API_TOKEN=your_envio_api_token_here
HYPERSYNC_URL=https://polygon.hypersync.xyz
